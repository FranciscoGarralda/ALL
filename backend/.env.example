# Backend Environment Variables
# Copy this file to .env and update with your values

# Database Configuration (PostgreSQL)
# For local development: postgresql://postgres:password@localhost:5432/sistema_financiero
# For Railway: Will be provided automatically
DATABASE_URL=postgresql://user:password@localhost:5432/database_name

# JWT Secret - IMPORTANT: Generate a secure key for production
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL for CORS
# Update with your Vercel URL in production
FRONTEND_URL=http://localhost:3000

# Optional: Admin credentials (for initial setup)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=admin123