# Frontend Environment Variables
# Copia este archivo a .env.local y configura los valores

# URL del backend (obligatorio)
NEXT_PUBLIC_API_URL=https://tu-backend.up.railway.app

# Backend Environment Variables (para Railway)
# NUNCA commitees valores reales, solo ejemplos

# Conexión a base de datos (Railway la proporciona automáticamente)
DATABASE_URL=postgresql://user:password@host:port/database

# Clave secreta para JWT (OBLIGATORIO - genera una única y segura)
# Puedes generar una con: openssl rand -base64 32
JWT_SECRET=CAMBIAR_ESTO_POR_UN_VALOR_SEGURO_Y_UNICO

# Contraseña del usuario admin inicial (OBLIGATORIO)
ADMIN_PASSWORD=CAMBIAR_ESTO_POR_UNA_CONTRASEÑA_SEGURA

# Entorno de ejecución
NODE_ENV=production

# Puerto del servidor (Railway lo asigna automáticamente)
PORT=8080